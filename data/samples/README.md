# Sample stacks

Git-only fixtures for M1+. Excluded from `meson dist` (see `.gitattributes`).

| File | Role |
|---|---|
| `recipes.yolodex` | Four-card kitchen stack (matches the List-view mock) |

Not installed with the `.deb`.
